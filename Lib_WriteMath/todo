- faire que quand on surligne un texte, on puisse faire click droit ou raccourci clavier et que ca permet de precess le truc (genre c'est une dérivation, hop ca remplace par la valeur de la dérivé)
ou alors ca met un menu deroulant et on peut process OU factorizer OU expand, etc
==> top du top

- faire que les mots clés genre let et given prennent une couleur différente
- faire que les noms de fonctions genre Print() ou Diff ou autre prennent une couleur différente
- faire que les méthodes d'objet prennent une couleur différente

- faire que quand une variable est déclarée on ne peut pas faire une réaffectation

- faire en sorte que quand on a un systeme d'équation qui est sous-contraint, genre 2 equations et une seule variable a trouver, ca essaye d'afficher le resultat pour chaque equation
genre
p = -q+9 = -6/q

- faire que quand on cree une fonction avec le constructeur Function()
  on peut faire des fonctions dont on sait pas ce que ca fait en faisant
  Function(x, Df->Ef), genre si ya pas de fleche ca fait comme la fonction sage function('f')(x)

- faudra implementer une fonction contructor DEquation() pour creer une equation differentielle
- faire en sorte que quand on tape (d/d_var)^k(f), ca corresponde a faire f.diff(var, k)

- faire en sorte de pouvoir afficher des graphes de fonction en utilisant geogebra
- faire en sorte de pouvoir afficher des trucs 3d

\text{e} = constante d'Euler
\text{i} = nombre imaginaire i
\text{q}_e = charge d'un electron

bon maintenant faut attaquer le parser
mais avant de faire le parseur, faut definir les regles, ca c'est ici: qu'est-ce qu'on veut ?
--------------------------------------------------------------------------------------------

on a differents objets:
- Fonctions
- valeurs littérales
- variables = Elements d'un ensemble
- equations = 2 membres séparés par un égal
- inequations = 2 membres séparés par un leq ou geq
- Bool = booleen
- 
